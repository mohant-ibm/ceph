overrides:
  ceph:
    fs: xfs
    log-ignorelist:
      - \(POOL_APP_NOT_ENABLED\)
      - \(OSD_DOWN\)
    conf:
      osd:
        osd objectstore: bluestore
        bluestore block size: 96636764160
